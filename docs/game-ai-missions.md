# Скрипты ИИ миссий

Собрано `tools/aiscript.py` (`make aiscripts`) по листингам.

`AiLoop` `$025282` каждый кадр уменьшает такт `$21(a5)`, и когда тот
дошёл до нуля, берёт номер этапа из байта `+$3` описания миссии и
прыгает по самоотносительной таблице `AiScriptTable` `$0252EE` —
64 записи. За ними 35 разных скриптов: часть этапов делит один на всех.

Скрипт — это список правил по приоритету, той же формы, что и всё
остальное поведение в игре: `bsr` правило, `bne` на общий выход. Правила
почти все сложены по одному шаблону:

    bsr.w   TestPhaseMask           ; работает ли правило в этой фазе
    jsr     (Player_024Fxx).l       ; можно ли её применить сейчас
    jsr     (CountEnemyUnitsNear).l ; сколько чужих рядом подходит под a6
    cmp.b   d5,d7 / bcs мимо        ; меньше d5 — не стоит того
    lea     (data_131).l,a6
    jsr     (CountOwnUnitsNear).l   ; своих под удар не подставлять
    tst.b   d7 / bne мимо
    ...                             ; анимация и оплата

Отсюда и смысл аргументов: `a6` — предикат «чем занят юнит», `d5` —
сколько таких надо набрать, `d7` — **маска фаз**, в которых правило
вообще работает (`TestPhaseMask` `$02AEB8` делает `btst` номера фазы
`$FFE0B3` в этой маске). Окно счёта — 12x6 клеток от курсора ИИ.

## Чем меряется «рядом»

`CountEnemyUnitsNear` `$05DF6E` и `CountOwnUnitsNear` `$05E060` —
один и тот же обход окна **12x6 клеток** от курсора ИИ, различаются
только границей по номеру слота: первый считает слоты до 107
(`UnitSlotsP1Nest`…`UnitSlotsP1`), второй — от 148 (`UnitSlotsP2`).
То есть правило требует набрать рядом **чужих** и не задеть
**своих**.

## Что противник умеет, по всем скриптам сразу

| шаг | вызовов | в скольких скриптах | фазы |
|---|---|---|---|
| `AiQuakeNearEnemies` — ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | 28 | 14 | 3, 4, 5 |
| `AiHeavyRainWet` — ЛИВЕНЬ по сырым клеткам | 24 | 13 | 1, 2, 3 |
| `AiPhaseStep` — выбрать фазу и запустить её цепочку | 24 | 24 | любая |
| `AiHeavyRainPair` — ЛИВЕНЬ, две проверки подряд | 23 | 12 | 4, 5 |
| `AiStormNearEnemies` — БУРЯ, если рядом не меньше d5 чужих по предикату a6 | 23 | 12 | 3, 4, 5 |
| `ClampMapCoords` — загнать координаты в границы карты | 22 | 22 | любая |
| `AiOpenPanel` — открыть панель d6 | 21 | 20 | 3, 4, 5 |
| `AiPhase1To2OnSight` — фаза 1 -> 2, если рядом виден юнит нужного вида | 20 | 20 | любая |
| `AiUpgradeUnit` — ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | 20 | 13 | 0, 1, 2, 3, 4, 5 |
| `AiRunCommand` — выполнить команду d0 | 18 | 17 | 4, 5 |
| `AiOnCooldown` — пауза: молчать, пока тик меньше $46(a5) | 16 | 16 | любая |
| `AiPanelOnPhase5` — в фазе 5 открыть панель ｼｾﾞﾝ | 16 | 16 | любая |
| `AiClampCursor` — загнать курсор ИИ обратно в карту | 14 | 14 | любая |
| `AiDroughtBare` — ЗАСУХА по голой земле | 14 | 14 | 2, 3, 4, 5, 6 |
| `AiLightningAtUnit` — МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | 14 | 14 | 2, 3, 4, 5 |
| `AiMeteorNearEnemies` — МЕТЕОРИТ | 14 | 14 | 4, 5 |
| `AiOrderNearUnits` — раздать юнитам действие из набора $016854 | 14 | 14 | любая |
| `AiUpdateBanMask` — пересчитать маску запрещённых команд $FFE0DD | 14 | 14 | любая |
| `AiDroughtTypes` — ЗАСУХА по маскам типов местности | 13 | 13 | 3, 4, 5 |
| `Player_0239B0` — не разобрано | 9 | 9 | любая |
| `Player_0239E8` — не разобрано | 9 | 9 | любая |
| `UnitLoop_02B720` — не разобрано | 6 | 4 | любая |
| `AiSetPanelAfterTick` — после тика d0 поставить панель d1 | 5 | 5 | любая |
| `TestIndexBitThenCall` — шлюз яиц: проверить запрет и положить вид d0 | 4 | 4 | любая |
| `Unit_02A194` — не разобрано | 4 | 1 | любая |
| `AiPlantOnTerrain` — РАСТЕНИЕ, если вокруг набралось клеток нужного типа | 3 | 3 | 3, 4, 5 |
| `AiCallIfPhase` — вызвать процедуру из a0 | 2 | 2 | 0, 1, 2 |
| `AiDroughtNearEnemies` — ЗАСУХА, если рядом не меньше d5 чужих по a6 | 2 | 2 | 2 |
| `AiPlantAtCursor` — РАСТЕНИЕ по типу клетки под курсором | 2 | 2 | 0, 1, 2, 3, 4, 5, 6, 7 |
| `AiQuakeNearEnemies2` — ЗЕМЛЕТРЯСЕНИЕ, вариант со своей проверкой применимости | 2 | 2 | 3, 4, 5 |
| `Player_02A922` — не разобрано | 2 | 2 | любая |
| `AiChainP1` — цепочка фазы 1 напрямую, мимо AiPhaseStep | 1 | 1 | любая |
| `AiDroughtGrowth` — ЗАСУХА по заросшим клеткам | 1 | 1 | 2, 3, 4, 5 |
| `AiLightningAtCursor` — МОЛНИЯ по клетке под курсором | 1 | 1 | 0, 1, 2, 3, 4, 5, 6, 7 |
| `AiMeteorOnEnemyUnits` — МЕТЕОРИТ по чужим, предикат $01E4A6 | 1 | 1 | любая |
| `AiPlanForPhase` — взять план текущей фазы | 1 | 1 | любая |
| `AiQuakeNearEnemies3` — ЗЕМЛЕТРЯСЕНИЕ, вариант с другой анимацией | 1 | 1 | 3, 4, 5 |
| `AiUnbanOnMoney` — снять запрет, когда денег больше порога | 1 | 1 | любая |
| `Core_009C48` — не разобрано | 1 | 1 | любая |
| `Player_0248CE` — оплатить ЗАСУХУ из кошелька второго | 1 | 1 | любая |
| `Player_02B8BC` — не разобрано | 1 | 1 | любая |
| `Random` — бросок кубика | 1 | 1 | любая |

## Что противнику открыто в какой фазе

| фаза | команды |
|---|---|
| 0 | МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| 1 | ЛИВЕНЬ, МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| 2 | ЗАСУХА, ЛИВЕНЬ, МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| 3 | БУРЯ, ЗАСУХА, ЗЕМЛЕТРЯСЕНИЕ, ЛИВЕНЬ, МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| 4 | БУРЯ, ЗАСУХА, ЗЕМЛЕТРЯСЕНИЕ, ЛИВЕНЬ, МЕТЕОРИТ, МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| 5 | БУРЯ, ЗАСУХА, ЗЕМЛЕТРЯСЕНИЕ, ЛИВЕНЬ, МЕТЕОРИТ, МОЛНИЯ, ПЕРЕДЕЛКА юнита: вариант d5, РАСТЕНИЕ |
| любая | МЕТЕОРИТ |

Порог `d5` — сколько чужих надо насчитать рядом: 52 раза при d5=3, 40 раз при d5=6, 39 раз при d5=8, 14 раз при d5=4, 5 раз при d5=0.

## Скрипты

Этапы, которые делят один скрипт, перечислены вместе. Подписи
миссий — по байту `+$3` их описаний.

### `$026D18` — этапы 9, 15, 17, 18, 31, 40, 46, 47, 48, 49, 50

Миссии: гл.1 м.22, гл.1 м.28, гл.1 м.32, гл.1 м.34, гл.1 м.36, гл.1 м.39.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$026F08` — этапы 51, 52, 53, 54, 55, 56, 57, 58

Миссии: гл.0 м.1, гл.0 м.2, гл.0 м.3, гл.0 м.4, гл.0 м.5, гл.0 м.6, гл.0 м.7, гл.0 м.8.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |

### `$0254EE` — этапы 0, 1, 2, 21, 22, 23

Миссии: гл.1 м.1, гл.1 м.2, гл.1 м.3, гл.1 м.4, гл.1 м.5.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |

### `$026F2A` — этапы 59, 60, 61, 62, 63

Ни одна миссия кампании на этих этапах не стоит.

Пусто: сразу `rts`, ИИ на этих этапах не делает ничего.

### `$02551A` — этапы 3, 4, 25

Миссии: гл.1 м.7, гл.1 м.8, гл.1 м.9.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фазы 4, 5 |
| `AiRunCommand` | выполнить команду d0 | d0=$4, фазы 4, 5 |

### `$025BEE` — этапы 38, 39

Миссии: гл.1 м.29, гл.1 м.40.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |

### `$025514` — этап 24

Миссии: гл.1 м.6.

| шаг | что | аргументы |
|---|---|---|
| `AiSetPanelAfterTick` | после тика d0 поставить панель d1 | — |
| `TestIndexBitThenCall` | шлюз яиц: проверить запрет и положить вид d0 *(по маске тика $7FC)* | — |

### `$025552` — этап 5

Миссии: гл.1 м.10.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фазы 4, 5 |

### `$025582` — этап 26

Миссии: гл.1 м.11.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фазы 4, 5 |
| `AiRunCommand` | выполнить команду d0 | d0=$4, фазы 4, 5 |
| `Unit_02A194` | — | — |
| `Unit_02A194` | — | — |
| `Unit_02A194` | — | — |
| `Unit_02A194` | — | — |

### `$02562A` — этап 28

Миссии: гл.1 м.12.

| шаг | что | аргументы |
|---|---|---|
| `TestIndexBitThenCall` | шлюз яиц: проверить запрет и положить вид d0 *(по маске тика $3FC)* | — |
| `AiSetPanelAfterTick` | после тика d0 поставить панель d1 | — |

### `$025630` — этап 7

Миссии: гл.1 м.13.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiDroughtNearEnemies` | ЗАСУХА, если рядом не меньше d5 чужих по a6 | a6=Ai_01E470, d5=$6, фаза 2 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фазы 3, 4, 5 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фазы 3, 4, 5 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 3, 4, 5 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фазы 4, 5, 6 |

### `$025754` — этап 8

Миссии: гл.1 м.14.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $3F8)* | d0=$4, фазы 4, 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=Ai_01E470, d5=$8, фаза 3 |

### `$02583A` — этап 27

Миссии: гл.1 м.21.

| шаг | что | аргументы |
|---|---|---|
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `Player_02B8BC` | — | — |
| `Core_009C48` | — | — |
| `Player_0248CE` | оплатить ЗАСУХУ из кошелька второго | — |

### `$02597E` — этап 34

Миссии: гл.1 м.17.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPlantAtCursor` | РАСТЕНИЕ по типу клетки под курсором *(по маске тика $FE)* | фазы 1, 2 |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `Player_02A922` | — | — |
| `AiDroughtGrowth` | ЗАСУХА по заросшим клеткам | a6=$025AD0, d5=$1500, фазы 2, 3, 4, 5 |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фазы 2, 3, 4, 5 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |

### `$025AEE` — этап 11

Миссии: гл.1 м.19.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiCallIfPhase` | вызвать процедуру из a0 | фазы 0, 1, 2 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фазы 1, 2 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фазы 3, 4, 5 |
| `AiRunCommand` | выполнить команду d0 | d0=$4, фазы 4, 5 |

### `$025C1C` — этап 13

Миссии: гл.1 м.26.

| шаг | что | аргументы |
|---|---|---|
| `AiPlanForPhase` | взять план текущей фазы | — |
| `AiChainP1` | цепочка фазы 1 напрямую, мимо AiPhaseStep | — |

### `$025C42` — этап 29

Миссии: гл.1 м.16.

| шаг | что | аргументы |
|---|---|---|
| `AiSetPanelAfterTick` | после тика d0 поставить панель d1 | — |
| `TestIndexBitThenCall` | шлюз яиц: проверить запрет и положить вид d0 *(по маске тика $7FC)* | — |

### `$025C48` — этап 14

Миссии: гл.1 м.31.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |
| `AiPlantOnTerrain` | РАСТЕНИЕ, если вокруг набралось клеток нужного типа | фазы 3, 4, 5 |
| `AiRunCommand` | выполнить команду d0 | d0=$4, фаза 5 |

### `$025C74` — этап 33

Миссии: гл.1 м.15.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$025C7A` — этап 10

Миссии: гл.1 м.38.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$10, d5=$0, фазы 0, 1, 2, 3, 4, 5 |

### `$025E36` — этап 42

Миссии: гл.1 м.27.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |
| `AiPlantOnTerrain` | РАСТЕНИЕ, если вокруг набралось клеток нужного типа | фазы 3, 4, 5 |

### `$025E58` — этап 35

Миссии: гл.1 м.20.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |
| `AiPlantOnTerrain` | РАСТЕНИЕ, если вокруг набралось клеток нужного типа | фазы 3, 4, 5 |
| `AiCallIfPhase` | вызвать процедуру из a0 | фазы 0, 1, 2 |

### `$025E9C` — этап 30

Миссии: гл.1 м.18.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фазы 4, 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `Player_02A922` | — | — |
| `AiQuakeNearEnemies3` | ЗЕМЛЕТРЯСЕНИЕ, вариант с другой анимацией | a6=Ai_0287B8, d5=$3, фазы 3, 4, 5 |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фазы 3, 4, 5 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фазы 3, 4, 5 |

### `$025F9E` — этап 41

Миссии: гл.1 м.42.

Правил нет — скрипт ничего не вызывает, только правит
состояние:

```asm
	lea	(Player2State).l,a4
	move.b	#$09,$F(a4)
	rts
```

### `$025FAC` — этап 44

Миссии: гл.1 м.24.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фазы 3, 4 |
| `AiOpenPanel` | открыть панель d6 | d6=$C, фаза 5 |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$6, d5=$4, фазы 1, 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$18, d5=$0, фазы 1, 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$6, d5=$0, фазы 1, 2, 3, 4, 5 |

### `$0260AC` — этап 45

Миссии: гл.1 м.41.

Правил нет — скрипт ничего не вызывает, только правит
состояние:

```asm
	lea	(SessionState).l,a0
	lea	(Player2State).l,a1
	lea	(Player1State).l,a2
	move.l	#$00199999,$2(a1)
	move.b	$F(a2),d0
	cmpi.b	#$07,d0
	bhi.w	loc_0260D8
	addq.b	#4,d0
	andi.b	#$07,d0
	move.b	d0,$F(a1)
	cmpi.b	#$0B,d0
	bne.w	loc_0260EA
	move.b	#$0C,$F(a1)
	move.b	$16(a2),d1
```

### `$02626C` — этап 32

Миссии: гл.1 м.35.

| шаг | что | аргументы |
|---|---|---|
| `TestIndexBitThenCall` | шлюз яиц: проверить запрет и положить вид d0 *(по маске тика $7FC)* | — |
| `Player_0239B0` | — | — |
| `Player_0239E8` | — | — |
| `AiSetPanelAfterTick` | после тика d0 поставить панель d1 | — |

### `$0262B4` — этап 6

Миссии: гл.1 м.37.

| шаг | что | аргументы |
|---|---|---|
| `AiSetPanelAfterTick` | после тика d0 поставить панель d1 | — |

### `$0262D0` — этап 12

Миссии: гл.1 м.33.

| шаг | что | аргументы |
|---|---|---|
| `AiLightningAtCursor` | МОЛНИЯ по клетке под курсором | фазы 0, 1, 2, 3, 4, 5, 6, 7 |
| `AiPlantAtCursor` | РАСТЕНИЕ по типу клетки под курсором | фазы 0, 1, 2, 3, 4, 5, 6, 7 |

### `$02632E` — этап 36

Миссии: гл.1 м.23.

| шаг | что | аргументы |
|---|---|---|
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiDroughtNearEnemies` | ЗАСУХА, если рядом не меньше d5 чужих по a6 | a6=Ai_01E470, d5=$6, фаза 2 |
| `AiQuakeNearEnemies2` | ЗЕМЛЕТРЯСЕНИЕ, вариант со своей проверкой применимости | a6=AiBusyRoamingNotSpecies, d5=$3, фазы 3, 4, 5 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$02652C` — этап 20

Миссии: гл.1 м.45.

| шаг | что | аргументы |
|---|---|---|
| `Random` | бросок кубика | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$0266F6` — этап 19

Миссии: гл.1 м.43.

| шаг | что | аргументы |
|---|---|---|
| `AiMeteorOnEnemyUnits` | МЕТЕОРИТ по чужим, предикат $01E4A6 | — |
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$026708` — этап 16

Миссии: гл.1 м.25.

| шаг | что | аргументы |
|---|---|---|
| `AiUnbanOnMoney` | снять запрет, когда денег больше порога | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$6, d5=$3, фазы 3, 4, 5 |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000000, d5=$4, фазы 2, 3, 4, 5 |

### `$0268EE` — этап 37

Миссии: гл.1 м.44.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$6, фазы 4, 5 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$4, фазы 4, 5 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фазы 4, 5 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фазы 4, 5 |
| `AiQuakeNearEnemies2` | ЗЕМЛЕТРЯСЕНИЕ, вариант со своей проверкой применимости | a6=AiBusyRoamingNotSpecies, d5=$4, фазы 4, 5 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$6, d5=$4, фазы 3, 4, 5 |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$6, d5=$3, фазы 1, 2 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$18, d5=$0, фазы 4, 5 |

### `$026ADE` — этап 43

Миссии: гл.1 м.30.

| шаг | что | аргументы |
|---|---|---|
| `Player_0239B0` | — *(по маске тика $7FC)* | — |
| `Player_0239E8` | — | — |
| `AiPhaseStep` | выбрать фазу и запустить её цепочку | — |
| `AiPanelOnPhase5` | в фазе 5 открыть панель ｼｾﾞﾝ | — |
| `AiClampCursor` | загнать курсор ИИ обратно в карту | — |
| `AiDroughtTypes` | ЗАСУХА по маскам типов местности | d5=$8, фазы 3, 4, 5 |
| `AiOpenPanel` | открыть панель d6 | d6=$8, фаза 4 |
| `AiRunCommand` | выполнить команду d0 *(по маске тика $7F8)* | d0=$4, фаза 4 |
| `AiMeteorNearEnemies` | МЕТЕОРИТ | фаза 5 |
| `AiOnCooldown` | пауза: молчать, пока тик меньше $46(a5) | — |
| `ClampMapCoords` | загнать координаты в границы карты | — |
| `AiPhase1To2OnSight` | фаза 1 -> 2, если рядом виден юнит нужного вида | — |
| `AiOrderNearUnits` | раздать юнитам действие из набора $016854 | — |
| `AiUpdateBanMask` | пересчитать маску запрещённых команд $FFE0DD | — |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoaming, d5=$6, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=AiBusyIdle, d5=$6, фаза 3 |
| `AiHeavyRainWet` | ЛИВЕНЬ по сырым клеткам | a6=AiBusyRoamingNotSpecies, d5=$3, фаза 3 |
| `AiStormNearEnemies` | БУРЯ, если рядом не меньше d5 чужих по предикату a6 | a6=TestSpeciesAndAction, d5=$3, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=AiBusyBroad, d5=$6, фаза 3 |
| `AiQuakeNearEnemies` | ЗЕМЛЕТРЯСЕНИЕ, если рядом не меньше d5 чужих по a6 | a6=Ai_028764, d5=$3, фаза 3 |
| `AiDroughtBare` | ЗАСУХА по голой земле | a6=Ai_01E470, d5=$8, фаза 3 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=AiBusyIdle, d5=$8, фаза 4 |
| `AiHeavyRainPair` | ЛИВЕНЬ, две проверки подряд | a6=TestSpeciesAndAction, d5=$3, фаза 4 |
| `AiLightningAtUnit` | МОЛНИЯ по юниту: наборы действий d0/d1, вероятность d2 | d0=$40, d1=$38, d2=$80, фазы 2, 3, 4, 5 |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000006, d5=$4, фазы 3, 4, 5 |
| `UnitLoop_02B720` | — | — |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$2000006, d5=$3, фазы 1, 2 |
| `AiUpgradeUnit` | ПЕРЕДЕЛКА юнита: вариант d5, фильтр d4 | d4=$18, d5=$0, фазы 4, 5 |

