# Открытые вопросы

Один список на весь разбор. Заведён, потому что секции «Чего я НЕ
установил» разошлись по двадцати пяти файлам и часть из них протухла:
ответ приходил из соседнего захода и в старую секцию не возвращался.

**Правило.** Вопрос живёт здесь. В тематическом файле остаётся только то,
что без окружающего текста непонятно, и ссылка сюда. Когда вопрос
закрывается, он переезжает в конец этого файла, в «Что закрылось», с
указанием, где теперь лежит ответ, — чтобы не разбирать его заново.

Машинных имён в ROM не осталось ни одного; всё ниже — про смысл, а не про
то, что ещё не прочитано.

## Запись юнита и блок параметров вида

- **`+$29` записи юнита.** Пишется степень удара, читателей нет ни одного
  ([game-damage.md](game-damage.md)).
- **90-байтовая запись** в [game-data.md](game-data.md): сам вопрос был
  сформулирован устаревше. Там разобрано не два поля, а около двадцати
  смещений, и сама запись оказалась **не описанием вида, а экранным
  объектом** со своими координатами и атрибутами спрайта. Осталось: какой
  экран ими пользуется и что значат остальные смещения.

## Виды: имена и родство

- **Как игра называет обитателей нейтральной зоны.** Таблицы имён для них
  нет вовсе: пять категорий — погода, динозавры, растения, приказы и цели
  маркировки ([game-neutral.md](game-neutral.md)).
- **Прямого доказательства привязки «вид → имя» нет.** Всё, что есть, —
  согласование независимых наблюдений ([game-species.md](game-species.md)).
- **Имён у видов 11, 12, 13 и 20 в ROM нет** — таблица имён обслуживает
  меню команд, а не подписи юнитов.
- **Почему виды 11 и 12 несут сторону A, а 14–16 и 18 — сторону B.** Для
  «диких» естественнее было бы не иметь ни одной.

## Взросление и улучшение

- **У взрослых форм защита лучше исходной** (160 у вида 8, 128 у вида 9
  против 175 у ｱﾛ), хотя по всем прочим осям взросление только добавляет.
- **У вида 9 `+$0A` = 5000 при `+$0C` = 1500.** Либо степени перепутаны
  при наборе данных, либо так задумано.
- **Улучшение отнимает у ﾋﾟｰﾁｬﾝ зрение и снижает порог размножения** —
  единственные два случая на всю таблицу.
- **У ﾌﾟﾃﾗ заполнен `+$C0` = 100**, хотя правила взросления у него нет.
- **Взрослому ﾃｨﾗﾉ дали зрение 3.** Мешает ли это на практике, не
  проверялось.

## ИИ

- **Байт `+$1` подзаписи фазы** (`$40`, `$80`, `$C0` у поздних фаз) не
  читает ни одна найденная команда. Похоже на вероятность, которую
  собирались бросать ([game-ai-player.md](game-ai-player.md)).
- **Чем наборы правил B и C отличаются по назначению.** Кто их запускает,
  установлено — скрипты миссий; зачем их два, нет.
- **`AiPhaseStepAlt` `$0295C4`** — полный двойник `AiPhaseStep` без
  проверки норм в фазе 1, площадок вызова ноль.
- **Почему вариантов землетрясения три** (`AiQuakeNearEnemies` и два
  других): различаются проверкой применимости и анимацией, но зачем
  столько — не ясно.
- **Правила, у которых маска есть, а порога нет, и наоборот.**
- **`RuleChaseMovingAsSpecies1` в скрипте ﾋﾟｰﾁｬﾝ** сработать не может:
  фильтр требует, чтобы сам юнит был вида 1. Ошибка правки — догадка.
- **Какие ещё поля состояния игрока участвуют в ИИ**, кроме `+$16`/`+$17`
  (точка сбора) и `+$18` (назначенный юнит).

## Местность и карта

- **Зачем проверка границ пропускает тип `$1F`.** Что при этом
  читается — известно ([game-movement.md](game-movement.md)): индекс 31
  уходит на `+$59`, в середину таблицы урона. Неизвестно, было ли это
  задумано, и вопрос почти академический: клеток типа 31 во всех
  исходных картах **две**.
- **Почему ИИ не обходит смертельную местность при выборе шага.**
  `TerrainIsHazard` стоит в пяти местах, и все они про клетку цели или
  напарника; `CanStepToCell` и обходы конуса на неё не смотрят
  ([game-damage.md](game-damage.md)).
- **Почему лёд (тип 14) стоит ｱﾛ 50**, когда всем прочим — 10.
- **Почему из набора местности ﾌﾟﾃﾗ выброшен ровно тип 21.**
- **Смысл флага `$FFE11F`.** Известно, кто взводит (`StageSpreadWideTick`
  `$02F1D2`) и что снимающих записей нет; известно следствие — рыба ﾌﾟﾃﾗ
  гаснет навсегда. Зачем — нет.

## Расстановка

- **Чем отличаются наборы 38–43 и 44–49.** Они совпадают байт в байт во
  всех трёх таблицах и рисуются одними и теми же кадрами, и ни одна
  строчка кода не сравнивает `+$0` с этими номерами. Похоже на дубликат,
  сделанный ради удобства сценариста, но зачем их два — неизвестно.
- **Откуда берутся 34 объекта на запрещённой местности.** `LoadPlacement`
  отказывается ставить на типы 22…30, но в данных 34 записи из 1204 туда
  и метят — то есть эти объекты не появляются никогда. Мусор в данных или
  расчёт на то, что местность к этому моменту изменится, — не выяснено.

## Экраны, текст, пароли

- **Почему глав девять, а две из них пустые.** Что пустоту знали и
  обходили — видно: `ChapterMissionFromPassword` правит номер главы
  `6 -> 8`, а заодно вычёркивает из паролей всю главу 2, у которой миссии
  есть ([game-passwords.md](game-passwords.md)). Зачем — по-прежнему нет.
- **Показывают ли экран напутствия режимы 2…8.** Селектор различает только
  «ноль или нет».
- **Кто говорит какую реплику в сценке.** Скрипты разобраны целиком и
  проигрываются ([game-cutscene-scripts.md](game-cutscene-scripts.md),
  `make cutscene CSARGS="--play 0"`). Что реплики берутся ПО ПОРЯДКУ,
  уже видно: при прогоне всех пятнадцати сценок число выполненных команд
  «реплика» точно совпало с числом записей текста, 196 из 196. А вот кто
  говорит — нет: номер актёра кладётся в `$C(a5)`, но что с ним делает
  `$0503B4`, не разобрано.
- **Зачем записи миссии список нейтральных типов**, если его никто не
  читает: вырезанная возможность или задел.
- **Что означают коды `a` и `u` на экране системной ошибки.** Байты
  различаются по векторам исключений, но строчных букв в шрифте нет, и
  рисуются они кружком и крупной ｵ. Мнемоника это или опечатка —
  неизвестно ([game-engine.md](game-engine.md)).

## Интерфейс разобран только начерно

Отдельного файла по интерфейсу нет, и имён по нему 115 штук вразнобой.
Что установлено сейчас:

- `ClearMapWindowPlane` `$00A7FC` (бывшее `DrawStatusLine`) ничего не
  рисует, а обнуляет 27 столбцов на 28 строк с `$E000` или с `$E01A`.
  Значит **окно карты 27 столбцов из сорока, панель 13**, и панель
  переезжает на другую сторону, когда `$24F2(a5)` больше `$10`.
- Графика: `$0111AA` — 106 тайлов панели в VRAM `$4AC0` (тайл `$256`),
  среди них ПОРТРЕТЫ ВИДОВ; `$0110AA` — 18 тайлов, уголки курсора,
  указатели и полоски. Оба выгружаются из `$004E9E`.
- `$012E06`, подложка меню, — ТРАФАРЕТ: сырой блок почти пуст, цвет ему
  дают при загрузке через `GfxDraw_00677C` с ключом `$EE`.

**Не разобрано:** какая плоскость под что отведена (регистры VDP не
проверены), раскладка панели, перекраска трафарета, сборка спрайтов
интерфейса (`BuildHudSprites` `$006564`), портреты (`LoadUnitPortrait`
`$006230`). До готового рисовальщика интерфейса отсюда далеко.

## Имена, которые придётся переделать

В банках `$03`–`$05` **350 имён из 732 описывают механику, а не
назначение**. `ScreenPushWorkBuffer04c` точно говорит, что процедура
делает, и ничего — какому экрану она понадобилась. Это сознательный
размен: дурное точное имя лучше красивого неверного. Если понадобится
работать со сценками, заставкой или экранами между миссиями всерьёз, этот
слой придётся проходить заново, уже от вызывающих.

## Что проверяется только запуском

Всё в проекте выведено из кода и подтверждено побайтовой пересборкой.
Пересборка доказывает, что я не сломал ROM, — и ничего не говорит о том,
верно ли я её прочитал. Вот что стоит прогнать на эмуляторе первым делом:

- **Работает ли `TickUnitIncome` на паузе и в меню.** Если нет, все
  секунды в разборе экономики надо считать по игровому времени.
- **Правда ли `TickLossCharge` портит деньги.** Вычитание там написано со
  сдвигом в байт; возможно, `+$1A` просто никогда не бывает ненулевым.
- **Правда ли `InputForMode3` и `InputForMode5` недостижимы.** В `$251A`
  пишут только 1, 2, 7 и 8 — но `bset #7` делает индекс отрицательным, и
  что тогда читается из таблицы, я не считал.
- **Нарезка по кадрам в главном цикле отключена маской `#$00`** —
  сознательное решение или след отладки.

## Что закрылось

Перечислено, чтобы не разбирать заново.

| было | ответ | где |
|---|---|---|
| `+$1A` состояния игрока | очередь списания: `AddLossCharge` кладёт, `TickLossCharge` тратит по единице за тик | [game-map.md](game-map.md) |
| «+50 разом» туда же | `TaxOpponentIfRich` `$02486E`: у противника больше `$1000` | там же |
| флаг `$FFE0D5` | `StageMorphP2StegoTick` `$02D7B8` взводит на тике `$3138`; это **этап 38, глава 1 миссия 40**; после него любой ｽﾃｺﾞ игрока 2 становится ﾃｨﾗﾉ | [game-events.md](game-events.md) |
| тип местности 20 | жерло, из него течёт лава типа 11 | [game-map.md](game-map.md) |
| типы 14, 17, 19 | лёд, пустошь, вода | [game-species-ptera.md](game-species-ptera.md) |

Оговорка к последней строке: типы 14, 17 и 19 опознаны **по совокупности**
(стоимость шага, что во что красит засуха, рыба ﾌﾟﾃﾗ на плитке `$53`), а не
найденной в ROM подписью. Прямого доказательства нет ни у одного из трёх.
| биты 0 и 1 `+$2F`, маска `$FFE060` | гасят анимацию типов 15 и 10: основа `$00078FFF`, бит 0 снимает тип 15, бит 1 — тип 10 (`SeedAnimatedTiles` `$0214D2`) | — |
| `$00C664` | `ScrollToUnitAndBeep`: подвести окно к юниту и дать звук `$A1` | [game-upgrade.md](game-upgrade.md) |
| `+$1E` состояния игрока | класс здоровья выбирает **портрет**, а не пиксели: `$003BCC` сверяет его с `$252E` и зовёт `LoadUnitPortrait` | [game-hp.md](game-hp.md) |
| `UnitIsEdible`, `$01E3E6`, `$01E422` | три видовых фильтра с разными масками: бит 7 `+$C`; `FilterLiveMeatEater` (3, 4, 6, 8, 9, 10); `FilterLiveSpeciesSet` (4, 6, 8, 9, 10) | [game-ai-player.md](game-ai-player.md) |
| `$01A812` | `CarcassExpire` | [game-neutral.md](game-neutral.md) |
| действия `$1E`, `$20`, `$25` | пастись, съесть, оглушение после броска (`StunAfterToss`) | [game-actions.md](game-actions.md) |
| `+$C4`…`+$CA` | степени удара улучшенного юнита | [game-upgrade.md](game-upgrade.md) |
| типы расстановки 1–4 | гнёзда: 1 — игрока 1, 2/3/4 — три разновидности гнезда игрока 2 (вид 0 в шаблоне) | [game-stages.md](game-stages.md) |
| поза расстановки 1 | `EnterNestPose`: поза гнезда, 230 из 232 объектов с ней — гнёзда | там же |
| поражение по `Player1State+$11` | это **Y первого гнезда игрока 1**, а не угол камеры; камера из него же, но через `ApplyScrollOrigins` | там же |
| оси в слове расстановки | младшие 6 бит — **Y**, старшие 6 — **X**; было наоборот, из-за чего `tools/maps.py` рисовал точки зеркально | там же |
| типы расстановки 25–49, 85–90 | запасные ростеры врага: те же виды 1–6 игрока 2 с другим блоком параметров и другой графикой, выбор через `+$0E…+$17` напутствия миссии | там же |
| что изображают виды 17, 18, 19 | ﾒｶﾞｻﾞｳﾙｽ: вид 18 — голова, вид 19 — загривок и четыре лапы, вид 17 — стеклянная капсула с фигурой внутри | [game-stages.md](game-stages.md) |
| почему вид 17 «левее» прямоугольника | он не левее, а **позади**: фигура стоит вертикально, а не горизонтально — прежняя запись была следствием перепутанных осей | там же |
| пары слов в `data_1625FA` | это таблица анимаций 0…3, общих для всех типов: четыре анимации по четыре одинаковых направления, рисуют только из общего банка | [game-graphics.md](game-graphics.md) |
| мясо это или яйца у вида 26 | **мясо**: типы 69–74 покадрово совпадают с динозаврами 5–10 вплоть до позы падали, тип 67 — груда костей, и все 26 объектов стоят с позами `$04`/`$08` | [game-neutral.md](game-neutral.md) |
| позы расстановки 2, 4, 8 | 2 — просто стоит; 4 — падаль навсегда (`MakeCarcass` плюс `+$14 = 0`); 8 — то же, но с битом 4 `+$11`, который читает только `CanRemodelUnit` | [game-stages.md](game-stages.md) |
| что несут виды 16 и 18 в `+$20` | ничего: это обычный партнёр по стычке, то есть убийца; вид 16 на нулевом HP вообще не гибнет, а вид 18 валит всё сооружение | [game-stages.md](game-stages.md) |
| где лежит графика шрифта | `FontTiles` `$010322`, сжатый блок метода 7: 5440 байт, 170 тайлов; выгружается тремя кусками по `FontUploadPlan` | [game-graphics.md](game-graphics.md) |
| как разбить `data_gfx` | надвое: `$0789E8`–`$150434` — 31 таблица банка и 3052 кадра встык без дыр, `$150434`–`$15E950` — 50 наборов анимаций по 232 байта и тела скриптов | [rom-map.md](rom-map.md) |
| смысл действия `$33` | вторая половина позы родов: своего обработчика нет, весь смысл в таймере `SetPoseRemainder` — остаток той же анимации после `$24` | [game-actions.md](game-actions.md) |
| номера обычных паролей | это не этапы, а ГЛАВА и МИССИЯ: 107 паролей на главы 0, 1, 3, 4, 5, 8; второй главы в паролях нет вовсе | [game-passwords.md](game-passwords.md) |
| что делают 16 особых | разобраны по `PasswordRunTable` `$047F38`; `staff` — концовка, `$rika` — прыжок на ноль, три слова на этом пути не делают ничего | там же |
| пароли `$` и `#` | обычные пароли главы 8, миссии 1 и 6; `$` записан дважды, и второй недостижим | там же |
| `+$A8` блока вида | сколько клеток подряд идти в одну сторону: 2, 3, 5, 5, 3, 2 | [game-movement.md](game-movement.md) |
| `+$B0`…`+$B3` | две пары шансов «базовый/улучшенный»: особый ответ принимающего удар и разрыв стычки; у ｱﾛ и ﾃｨﾗﾉ все четыре нули | [game-damage.md](game-damage.md) |
| бит 6 байта `+$C` | его там нет: все три записи в `+$C` маскируют `$1F` и ставят только биты 5 и 7; бит 6 принадлежит байту ШАБЛОНА и давно разобран | [game-species.md](game-species.md) |
| кто создаёт типы 69–76 | никто: и команда ﾀﾏｺﾞ, и ИИ берут тип из ростера миссии, а там этих номеров нет никогда | [game-stages.md](game-stages.md) |
| вторая пара порогов `+$D0`/`+$D4` | внешняя развилка — приказ ﾊﾝｼｮｸ (`IsOrderBreed`), внутренняя — бит 5 `+$11`; приказ опускает порог на 200…400 | [game-actions.md](game-actions.md) |
| `+$CB` и `+$CD` | это не поля, а младшие половинки СЛОВ `+$CA` и `+$CC`; оба слова разобраны давно | [game-damage.md](game-damage.md) |
| ограничено ли лечение максимумом | нет: все четыре площадки вызова `HpHeal` ничего не зажимают, потолок только `$FFFF` | [game-hp.md](game-hp.md) |
| чем отличаются виды 7 и 10 | ничем: один скрипт на двоих и общий с видом 1 блок. Главное другое: ни один из четырёх способов создать юнита их не даёт | [game-species.md](game-species.md) |
| шесть цен на `$0223A4` | цена УЛУЧШЕНИЯ по виду юнита; у игрока 2 индекс сдвинут на единицу — похоже на ошибку | [game-actions.md](game-actions.md) |
| полярность `FilterByMask` | годится **враг вида ИЗ маски** (`not.l` перед `btst`): 3, 4, 5, 8, 9, 10, 11, 12, 14, 15 — хищники и дикие. Правило не подходит, а шарахается в случайную сторону; стоит у видов 1, 2, 6, 20 | [game-ai-core.md](game-ai-core.md) |
| что считает `$FFE096` | это `SessionState + $10`, первый байт переписи игрока 2 — сколько у него стегозавров; при двух и меньше часы переводятся на событие превращения | [game-species-stego.md](game-species-stego.md) |
| обработчики действий в `ActionJumpTable` | все одиннадцать разобраны и сведены в таблицу; действие `$25` просто истекает в `$1A` с удвоенным таймером, `$09` — сам момент вылупления | [game-ai.md](game-ai.md) |
| какая палитра какому типу | таблица `UnitPaletteRow` `$015156` по номеру типа: ряд 0 гнёзда, 1 игрок 1, 2 игрок 2, 3 — ﾒｶﾞｻﾞｳﾙｽ и вид 16; сами ряды собирает `BuildStagePalettes` | [game-graphics.md](game-graphics.md) |
| нумерация подпунктов команд | ﾀﾏｺﾞ и ｼｮｸﾌﾞﾂ имеют таблицы перевода (`EggMenuToRoster`, `PlantMenuToTerrain`), ｶｲｿﾞｳ — прямой порядок, а у ｼｾﾞﾝ таблицы нет вовсе — семь отдельных обработчиков | [game-map.md](game-map.md) |
| два номера этапа | байт `+$3` описания миссии индексирует сценарий, вход и скрипт ИИ **как есть**, а `StageTable` — **минус один**; `tools/maps.py` из-за этого подписывал каждую карту соседней миссией | [game-stages.md](game-stages.md) |
| неопознанные типы расстановки | не осталось ни одного: 1202 объекта разнесены по шести наборам | там же |
| что нарисовано в банках графики | разобрана вся цепочка «тип → банк → скрипт → кадр» и сами картинки: гнёзда, клоны Профессора, тени, засады | [game-graphics.md](game-graphics.md) |
| формат скрипта анимации | записи по три байта плюс восемь команд `$FF…$F8`; бит 8 слова кадра переключает на общий банк | там же |
| назначение редких трапов | разобраны все пятнадцать: `$FF31`/`$FF33` — затухание музыки «со стойкой» и без, `$FF23`/`$FF2A` — два текстовых окна, `$FF2C` — яркость палитры, `$FF11` — распаковка кадра спрайта | [game-engine.md](game-engine.md) |
| где читаются биты 6 и 7 `+$11` | в `MakeUnitNameWord`: входят в ключ кэша плиток, а после распаковки выбирают трафарет — прореживание тетрад для утопленного и набор `$4AF8`/`$4BF8` для «за растениями» | там же |
| есть ли в игре экран краха | есть: четыре вектора исключений печатают «ｼｽﾃﾑｴﾗｰ X ﾃﾞｽ» на красном фоне и виснут, плюс пятый путь — «ｱｷPCGｶﾞﾐﾂｶﾗﾅｲ» из `AllocTileSlot`, когда кончились сорок слотов плиток | там же |
| что на самом деле лежит в байте клетки карты | **не тип**: у карты две жизни. Правила берут ТИП из таблицы перевода `$FFBBBC`, которая приходит с набором графики этапа; картинка строится по самому байту и типа не знает. Прежние проценты по типам были посчитаны не про то | [game-maps.md](game-maps.md), `tools/maptex.py` |
| как карта превращается в картинку | **мимо типа**: `DrawMapColumn` берёт байт прямо из буфера `$FF9FC6`, вычитает единицу и идёт в таблицу «байт -> четыре метатайла -> четыре тайла -> имя плюс `$6075`». Клетка = 4x4 тайла, 32x32 точки. Кромку `BuildMapEdgeCodes` досчитывает по восьми соседям для байтов `$19` и `$49` | `tools/maptex.py` |
| как выглядят типы местности | `python tools/maptex.py --types` рисует 32 x 9: строка — тип, столбец — набор графики. Имён в ROM по-прежнему нет, но картинка есть | [game-tilesets.md](game-tilesets.md) |
| что означает байт позы расстановки | номер АНИМАЦИИ, через цепочку сравнений в `LoadPlacement`: 0 — ход (`$0A`/`$0B` по чётности направления), 1 — гнездо (`$06`), 2/3/7 — стойка (`$05`), 4/8 — падаль (`$20`, у декора `$06`). Шести разбираемых поз в данных нет ни одной | `tools/maptex.py` |
| чем рисуется огонь | не плиткой набора: у клетки типа 12 это шестнадцать раз голая земля. `DrawCellTiles` ловит байт карты `$14` и уходит в `DrawFireCell` `$015D18`, который собирает клетку сам из двух плиток пламени `$0372`/`$0373` палитрой ряда 0 по маске из `FireTilePattern`. Сами плитки — последние 64 байта `SharedTiles` `$010A6C`, анимирует их `TickFireTiles` `$00B7EE` двумя кадрами | `tools/maptex.py` |
