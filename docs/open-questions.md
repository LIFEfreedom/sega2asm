# Открытые вопросы

Один список на весь разбор. Заведён, потому что секции «Чего я НЕ
установил» разошлись по двадцати пяти файлам и часть из них протухла:
ответ приходил из соседнего захода и в старую секцию не возвращался.

**Правило.** Вопрос живёт здесь. В тематическом файле остаётся только то,
что без окружающего текста непонятно, и ссылка сюда. Когда вопрос
закрывается, он переезжает в конец этого файла, в «Что закрылось», с
указанием, где теперь лежит ответ, — чтобы не разбирать его заново.

Машинных имён в ROM не осталось ни одного; всё ниже — про смысл, а не про
то, что ещё не прочитано.

## Запись юнита и блок параметров вида

- **`+$29` записи юнита.** Пишется степень удара, читателей нет ни одного
  ([game-damage.md](game-damage.md)).
- **`+$B0`…`+$B3` блока вида.** Четыре байта: у ｽﾃｺﾞ 25, 50, 25, 125, у
  ﾋﾟｰﾁｬﾝ 50, 128, 50, 80. Потребитель не найден.
- **`+$A8`.** 3 у ﾄﾘｹﾗ, 5 у ｱﾛ и ﾃｨﾗﾉ. Влияет ли хоть на что-нибудь —
  неизвестно.
- **Вторая пара порогов размножения `+$D0`/`+$D4`.** Выбор идёт по биту 5
  `+$11`, но какая из пары берётся в какой ветке, не проверено.
- **Бит 6 байта вида `+$C`.** Проверяется при создании юнита; биты 5 и 7
  разобраны ([game-species.md](game-species.md)).
- **`+$CB`…`+$CD` блока вида.** `+$C4`…`+$CA` закрылись (степени удара
  улучшенного), эти три — нет.
- **90-байтовая запись динозавра** в [game-data.md](game-data.md)
  разобрана на **два поля из девяноста** — `+$49` и `+$4D`. Самый крупный
  неразобранный кусок структуры во всём проекте.

## Действия и здоровье

- **Смысл действия `$33`.** Из прежнего списка `$1E`, `$20`, `$25` и `$33`
  первые три закрылись, четвёртое нет ([game-actions.md](game-actions.md)).
- **Ограничивается ли лечение максимумом** где-то выше по стеку
  ([game-hp.md](game-hp.md)).

## Виды: имена и родство

- **Как игра называет обитателей нейтральной зоны.** Таблицы имён для них
  нет вовсе: пять категорий — погода, динозавры, растения, приказы и цели
  маркировки ([game-neutral.md](game-neutral.md)).
- **Прямого доказательства привязки «вид → имя» нет.** Всё, что есть, —
  согласование независимых наблюдений ([game-species.md](game-species.md)).
- **Чем виды 7 и 10 отличаются друг от друга** и зачем им отдельный
  короткий обработчик. Блок параметров у обоих общий с видом 1.
- **Имён у видов 11, 12, 13 и 20 в ROM нет** — таблица имён обслуживает
  меню команд, а не подписи юнитов.
- **Мясо это или яйца у вида 26** ([game-neutral.md](game-neutral.md)).
- **Кто создаёт типы 69–76.** Цепочка от команды ﾀﾏｺﾞ до `CreateUnit`
  найдена, но номер типа приходит извне разобранного участка.
- **Почему виды 11 и 12 несут сторону A, а 14–16 и 18 — сторону B.** Для
  «диких» естественнее было бы не иметь ни одной.

## Взросление и улучшение

- **У взрослых форм защита лучше исходной** (160 у вида 8, 128 у вида 9
  против 175 у ｱﾛ), хотя по всем прочим осям взросление только добавляет.
- **У вида 9 `+$0A` = 5000 при `+$0C` = 1500.** Либо степени перепутаны
  при наборе данных, либо так задумано.
- **Улучшение отнимает у ﾋﾟｰﾁｬﾝ зрение и снижает порог размножения** —
  единственные два случая на всю таблицу.
- **У ﾌﾟﾃﾗ заполнен `+$C0` = 100**, хотя правила взросления у него нет.
- **Взрослому ﾃｨﾗﾉ дали зрение 3.** Мешает ли это на практике, не
  проверялось.

## ИИ

- **Что делают конечные действия дальше первых инструкций.** `$01CCFA`
  разобран (`KillUnit`), прочие — нет.
- **Полярность `FilterByMask` `$019230`** и маски `$0000DF38`: кого
  правило зовёт подойти, своего или чужого.
- **Байт `+$1` подзаписи фазы** (`$40`, `$80`, `$C0` у поздних фаз) не
  читает ни одна найденная команда. Похоже на вероятность, которую
  собирались бросать ([game-ai-player.md](game-ai-player.md)).
- **Чем наборы правил B и C отличаются по назначению.** Кто их запускает,
  установлено — скрипты миссий; зачем их два, нет.
- **`AiPhaseStepAlt` `$0295C4`** — полный двойник `AiPhaseStep` без
  проверки норм в фазе 1, площадок вызова ноль.
- **Почему вариантов землетрясения три** (`AiQuakeNearEnemies` и два
  других): различаются проверкой применимости и анимацией, но зачем
  столько — не ясно.
- **Правила, у которых маска есть, а порога нет, и наоборот.**
- **`RuleChaseMovingAsSpecies1` в скрипте ﾋﾟｰﾁｬﾝ** сработать не может:
  фильтр требует, чтобы сам юнит был вида 1. Ошибка правки — догадка.
- **Какие ещё поля состояния игрока участвуют в ИИ**, кроме `+$16`/`+$17`
  (точка сбора) и `+$18` (назначенный юнит).

## Местность и карта

- **Почему ИИ не обходит смертельную местность при выборе шага.**
  `TerrainIsHazard` стоит в пяти местах, и все они про клетку цели или
  напарника; `CanStepToCell` и обходы конуса на неё не смотрят
  ([game-damage.md](game-damage.md)).
- **Почему проверка границ пропускает тип `$1F`**, который индексирует за
  пределы таблицы ([game-movement.md](game-movement.md)).
- **Почему лёд (тип 14) стоит ｱﾛ 50**, когда всем прочим — 10.
- **Почему из набора местности ﾌﾟﾃﾗ выброшен ровно тип 21.**
- **Шесть цен на `$0223A4`** — за что ([game-map.md](game-map.md)).
- **Смысл флага `$FFE11F`.** Известно, кто взводит (`StageSpreadWideTick`
  `$02F1D2`) и что снимающих записей нет; известно следствие — рыба ﾌﾟﾃﾗ
  гаснет навсегда. Зачем — нет.
- **Что считает `$FFE096`** ([game-species-stego.md](game-species-stego.md)).

## Составной объект ﾒｶﾞｻﾞｳﾙｽ

- **Что несут виды 16 и 18 в поле `+$20`** и кто это туда кладёт.
- **Что изображают виды 17, 18, 19 на экране.** Анимации разобраны как
  номера, не как картинки.
- **Почему вид 17 стоит на две клетки левее прямоугольника.**
- **Почему поражение меряется по `Player1State+$11`.**
  `TickSpecies18March` `$01F570` сравнивает строку зверя с `$FF9C95`
  минус один, а это желаемый угол окна игрока 1 из `ApplyScrollOrigins`.
  Связь между «зверь дошёл» и «камера там» не объяснена.

## Экраны, текст, пароли

- **Что делает каждый из шестнадцати особых паролей**: обработчики —
  короткие переходники в банке `$05`.
- **Соответствие обычных номеров паролей этапам.** Номер кладётся в
  `$FFE45A`, потребитель не прослежен.
- **Почему в списке паролей есть строки из одного символа `$` и `#`.**
- **Где лежит графика шрифта.** Номера тайлов известны (`$21+`, `$780+`),
  какой ассет их грузит — нет.
- **Назначение трапов, вызываемых по одному-два раза.**
- **Почему глав девять, а две из них пустые.**
- **Показывают ли экран напутствия режимы 2…8.** Селектор различает только
  «ноль или нет».
- **Зачем записи миссии список нейтральных типов**, если его никто не
  читает: вырезанная возможность или задел.
- **Нумерация подпунктов команд** сверена только для приказов; для
  остальных порядок взят из меню.

## ROM за пределами кода

- **Пары слов в `data_1625FA`** и зачем к ним ведёт корень `$078804`.
- **Как разбить `data_gfx` на сегменты.** Уровней три, кадров 5 464, но
  вторая половина массива (записи 48–115) устроена иначе
  ([rom-map.md](rom-map.md)).

## Имена, которые придётся переделать

В банках `$03`–`$05` **350 имён из 732 описывают механику, а не
назначение**. `ScreenPushWorkBuffer04c` точно говорит, что процедура
делает, и ничего — какому экрану она понадобилась. Это сознательный
размен: дурное точное имя лучше красивого неверного. Если понадобится
работать со сценками, заставкой или экранами между миссиями всерьёз, этот
слой придётся проходить заново, уже от вызывающих.

## Что проверяется только запуском

Всё в проекте выведено из кода и подтверждено побайтовой пересборкой.
Пересборка доказывает, что я не сломал ROM, — и ничего не говорит о том,
верно ли я её прочитал. Вот что стоит прогнать на эмуляторе первым делом:

- **Работает ли `TickUnitIncome` на паузе и в меню.** Если нет, все
  секунды в разборе экономики надо считать по игровому времени.
- **Правда ли `TickLossCharge` портит деньги.** Вычитание там написано со
  сдвигом в байт; возможно, `+$1A` просто никогда не бывает ненулевым.
- **Правда ли `InputForMode3` и `InputForMode5` недостижимы.** В `$251A`
  пишут только 1, 2, 7 и 8 — но `bset #7` делает индекс отрицательным, и
  что тогда читается из таблицы, я не считал.
- **Нарезка по кадрам в главном цикле отключена маской `#$00`** —
  сознательное решение или след отладки.

## Что закрылось

Перечислено, чтобы не разбирать заново.

| было | ответ | где |
|---|---|---|
| `+$1A` состояния игрока | очередь списания: `AddLossCharge` кладёт, `TickLossCharge` тратит по единице за тик | [game-map.md](game-map.md) |
| «+50 разом» туда же | `TaxOpponentIfRich` `$02486E`: у противника больше `$1000` | там же |
| флаг `$FFE0D5` | `StageMorphP2StegoTick` `$02D7B8` взводит на тике `$3138`; это **этап 38, глава 1 миссия 40**; после него любой ｽﾃｺﾞ игрока 2 становится ﾃｨﾗﾉ | [game-events.md](game-events.md) |
| тип местности 20 | жерло, из него течёт лава типа 11 | [game-map.md](game-map.md) |
| типы 14, 17, 19 | лёд, пустошь, вода | [game-species-ptera.md](game-species-ptera.md) |

Оговорка к последней строке: типы 14, 17 и 19 опознаны **по совокупности**
(стоимость шага, что во что красит засуха, рыба ﾌﾟﾃﾗ на плитке `$53`), а не
найденной в ROM подписью. Прямого доказательства нет ни у одного из трёх.
| биты 0 и 1 `+$2F`, маска `$FFE060` | гасят анимацию типов 15 и 10: основа `$00078FFF`, бит 0 снимает тип 15, бит 1 — тип 10 (`SeedAnimatedTiles` `$0214D2`) | — |
| `$00C664` | `ScrollToUnitAndBeep`: подвести окно к юниту и дать звук `$A1` | [game-upgrade.md](game-upgrade.md) |
| `+$1E` состояния игрока | класс здоровья выбирает **портрет**, а не пиксели: `$003BCC` сверяет его с `$252E` и зовёт `LoadUnitPortrait` | [game-hp.md](game-hp.md) |
| `UnitIsEdible`, `$01E3E6`, `$01E422` | три видовых фильтра с разными масками: бит 7 `+$C`; `FilterLiveMeatEater` (3, 4, 6, 8, 9, 10); `FilterLiveSpeciesSet` (4, 6, 8, 9, 10) | [game-ai-player.md](game-ai-player.md) |
| `$01A812` | `CarcassExpire` | [game-neutral.md](game-neutral.md) |
| действия `$1E`, `$20`, `$25` | пастись, съесть, оглушение после броска (`StunAfterToss`) | [game-actions.md](game-actions.md) |
| `+$C4`…`+$CA` | степени удара улучшенного юнита | [game-upgrade.md](game-upgrade.md) |
